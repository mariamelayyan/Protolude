namespace ColliderEventSystem
{
    /// <summary>
    /// Whether a value gets replaced outright (Set) or added to the current value (Additive).
    /// </summary>
    public enum ValueMode
    {
        Set,
        Additive,
    }
}
